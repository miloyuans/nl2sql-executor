apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nl2sql-executor-data
  namespace: nl2sql
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # 按你的集群情况指定 storageClassName；不需要时删除这一行。
  # storageClassName: standard
